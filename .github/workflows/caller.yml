name: First Hello World Workflow

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reuseable.yml
    with:
      print: "Naveen, Deekshith, A"
